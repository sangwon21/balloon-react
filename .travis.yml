language: generic

sudo: required

services:
  - docker

before_install:
  - docker build -t junothyme/react-test-app -f ./frontend/Dockerfile.dev ./frontend

script:
  - docker run -e CI=true junothyme/react-test-app npm test

after_success:
  - docker build -t junothyme/docker-frontend ./frontend
  - docker build -t junothyme/docker-backend ./backend
  - docker build -t junothyme/docker-nginx ./nginx

  - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_ID" --password-stdin

  - docker push junothyme/docker-frontend
  - docker push junothyme/docker-backend
  - docker push junothyme/docker-nginx